cd ai-service && npm install && cp .env.example .env && npm run dev
cd backend && npm install && cp .env.example .env
# set ADMIN_API_KEY, RELAYER_PRIVATE_KEY, RELAYER_RPC_URL, CONTRACTS_DEPLOYMENTS_PATH
npm run dev
cd frontend && npm install && cp .env.example .env
# set NEXT_PUBLIC_BACKEND_URL and ADMIN_API_KEY (server-only)
npm run dev
cd frontend && npm install && cp .env.example .env
# set NEXT_PUBLIC_BACKEND_URL and ADMIN_API_KEY (server-only)
npm run dev
3AUTO_SETTLE=false         # set true to auto-settle via relayer
RELAYER_PRIVATE_KEY=0x...
RELAYER_RPC_URL=...
CONTRACTS_DEPLOYMENTS_PATH=../contracts/deployments/addresses.json
AI_SERVICE_URL=http://localhost:5001
